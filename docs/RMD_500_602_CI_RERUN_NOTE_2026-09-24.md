# RMD-500 / RMD-602 evidence CI rerun note — 2026-09-24

This note exists to create a fresh exact-head validation after a duplicate pull-request Rust test lane failed on the documentation-only RMD-500/RMD-602 evidence branch while the push Rust lane for the same exact head passed.

The failed duplicate PR run was CI `36067440842`, job `107860388447`, with `worker_cancel_tests::durable_cancel_stops_active_work_and_persists_terminal_state` observing an empty cancellation list. The same exact head `5f1caa665394650b7a0cf2eac9733018a80a7a0c` passed the push CI run `36067336918`, including the same `cargo test --workspace --all-features` job.

Ralph Bridge v0.1.0 reported that Actions write permission is not available for rerunning the single failed job, so this small documentation addendum intentionally creates a new branch head for normal exact-head CI qualification. It does not alter production code or canonical checklist state.
