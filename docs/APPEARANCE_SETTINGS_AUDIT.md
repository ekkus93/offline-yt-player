# OYP-1605 Appearance Settings Audit

The appearance policy provides the required Dark, Light, and System selector with Dark as the deterministic default. It also exposes the Library layout preference as a bounded List/Grid choice.

`AppearanceSettingsPolicyTest` qualifies the default and complete selector surfaces.
