# Sanitized YouTube provider fixtures

Bounded synthetic fixtures model representative YouTube player-response shapes without copied provider payloads, signatures, tokens, or user data. Production parser tests consume these fixtures directly.
